/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.dummyjson.com"],
  },
};

export default nextConfig;

// // next.config.js (updated)
// const nextConfig = {
//   images: {
//     domains: ["cdn.dummyjson.com"],
//   },
// };

// export default nextConfig;
